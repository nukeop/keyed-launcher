{
  "name": "keyed-launcher-monorepo",
  "version": "1.0.2",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter launcher dev",
    "build": "pnpm --filter launcher build",
    "preview": "pnpm --filter launcher preview",
    "tauri": "pnpm --filter launcher tauri",
    "tauri:dev": "pnpm --filter launcher tauri:dev",
    "tauri:build": "pnpm --filter launcher tauri:build",
    "test": "pnpm --filter launcher test",
    "test:coverage": "pnpm --filter launcher test:coverage",
    "test:watch": "pnpm --filter launcher test:watch",
    "lint": "pnpm --filter launcher lint",
    "lint:fix": "pnpm --filter launcher lint:fix",
    "prepare": "husky",
    "install:all": "pnpm install",
    "clean": "pnpm --filter launcher clean"
  },
  "author": {
    "name": "nukeop <12746779+nukeop@users.noreply.github.com>"
  },
  "license": "AGPL-3.0",
  "description": "High-performance multiplatform launcher built with Tauri - monorepo",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.1.2",
    "prettier": "^3.6.1"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx}": [
      "eslint --fix"
    ],
    "*.{ts,tsx,js,jsx,json,css,md}": [
      "prettier --write"
    ]
  }
}